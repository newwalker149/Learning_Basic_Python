#write a function called chop
#the function take a list and and modifies
#removing the first and last elements and returns none
#then write a fucntion called middle that take a list and returns a new list that contains all but the first and last elements

#how to do it
#check the length of the list
#use for to iterate all the list_chop
#after the iteration finish than


list = ['one', 'two', 'three', 'four']
def chop():
    total_list = len(list)
    list_chop = del list[-1]

    print(list_chop)




    #del list[0][-1]
    #return None
    #print(list)

chop()
